#include <Arduino.h>
#include <AwesomeSerial.h>
#include <SPT.h>

#define SER 9
#define CLK 10
#define RCLK 11

void setup() {
  pinMode(SER, OUTPUT);
  pinMode(CLK, OUTPUT);
  pinMode(RCLK, OUTPUT);
}

void loop() {
}
